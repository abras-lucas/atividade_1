#include <stdio.h>
#include <math.h>

float exponenciacao( float N1, float N2){

float resultado;
resultado = pow(N1, N2);
return resultado;

}

int main()
{
    float x, y;
    int opcao = 1;
    
    printf("Bem-vindo!!!");
    printf("\nInsira o primeiro numero:");
    scanf("%f", &x);
    printf("Insira o segundo numero:");
    scanf("%f", &y);
    
    while(opcao != 0){
        
        printf("Qual operacao deseja realizar?\n");
        printf("1 - Soma\n 2 - Subtracao\n 3 - Multiplicacao\n 4 - Divisao\n  5 - Exponeciacao\n");
        printf("Opcao: ");
        scanf("%i", &opcao);
        
        switch(opcao){
            
            case 1:
            break;
            case 2:
            break;
            case 3:
            break;
            case 4:
            break;
            case 5:
             
            resultado = exponenciacao(x,y);
            printf("\nResultado da exponenciacao: %.1f", resultado);

            break;
            default:
            break;
            
        }
        
    }

    return 0;
}
